Create a Python CLI app that:
1. Manages a simple to-do list.
2. Allows the user to add tasks, view tasks, and mark them as complete.
3. Uses a text file `tasks.txt` to save and load tasks so they persist.